export function fetchFunction() {
  const name = "Marzya Choco Mint CH";
  const originalName = "Marzya Luis";
  const gender = "Female";
  const uid = "0837e4be-b7ef-4bef-9b12-1e03f1f00a77";
  const isActive = true;
  const securityProtocol = "SHA-256";

  console.log(`
     Vtuber name : ${name}
     Vtuber original name : ${originalName}
     Uid : ${uid}
     Active status : ${isActive}
     Security Protocol : ${securityProtocol}`);
}
